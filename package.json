{
  "name": "edje-spinner",
  "version": "1.0.0",
  "description": "",
  "main": "edje-spinner.js",
  "scripts": {
    "start": "web-dev-server --node-resolve --open --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@web/dev-server": "^0.4.5"
  }
}
